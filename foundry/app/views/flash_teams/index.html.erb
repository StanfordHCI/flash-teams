<h1>Listing Flash Teams</h1>
 
<table>
  <tr>
    <th>ID</th>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
 
  <% @flash_teams.each do |flash_team| %>
    <tr>
      <td><%= flash_team.id %></td>
      <td><%= flash_team.name %></td>
      <td><%= link_to 'Show', flash_team %></td>
      <td><%= link_to 'Edit', edit_flash_team_path(flash_team) %></td>
      <td><%= link_to 'Destroy', flash_team_path(flash_team), method: :delete, data: { confirm: 'Are you sure you want to delete this flash team?' } %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'New Flash Team', new_flash_team_path %>