<%= javascript_include_tag "authoring/gdrive" %>
<%= javascript_include_tag "https://apis.google.com/js/client.js?onload=handleClientLoad" %>
<%= javascript_include_tag "https://apis.google.com/js/api.js"%>

<input type="button" id="submitButton" value="Start Google Drive!"></input>

<%= form_for @flash_team do |f| %>
  <% if @flash_team.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@flash_team.errors.count, "error") %> prohibited
      this flash team from being created:</h2>
    <ul>
    <% @flash_team.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name, :id => "nameVal" %>
  </p>

  <p>
    <%= f.label :json %><br>
    <%= f.text_area :json %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>

<script>
  var submitButton = document.getElementById("submitButton");
  submitButton.addEventListener("click", function(evt){
    // overallFolder = null;
    createNewFolder("New Flash Team")});
</script>
